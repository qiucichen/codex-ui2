# AI 报告工作台

这是一个基于 FastAPI 的模拟前后端原型：后端维护会话状态和模拟报告数据，前端通过接口加载历史报告、切换报告、切换绑定数据，并发送对话请求刷新大纲与详情预览。

## 运行

```bash
python3 -m pip install -r requirements.txt
npm run start
```

启动后打开：

```text
http://localhost:4173
```

## 检查

```bash
npm run check
```
